# 📱 Полная инструкция по настройке Telegram уведомлений

## Содержание
1. [Создание Telegram бота](#1-создание-telegram-бота)
2. [Получение Bot Token](#2-получение-bot-token)
3. [Получение Chat ID для пользователей](#3-получение-chat-id-для-пользователей)
4. [Настройка бота в системе](#4-настройка-бота-в-системе)
5. [Добавление пользователей и их Chat ID](#5-добавление-пользователей-и-их-chat-id)
6. [Настройка типов уведомлений](#6-настройка-типов-уведомлений)
7. [Настройка ежедневного дайджеста](#7-настройка-ежедневного-дайджеста)
8. [Тестирование уведомлений](#8-тестирование-уведомлений)
9. [Устранение неполадок](#9-устранение-неполадок)

---

## 1. Создание Telegram бота

### Шаг 1.1: Откройте Telegram
- Запустите приложение Telegram на телефоне или компьютере

### Шаг 1.2: Найдите @BotFather
- В поиске Telegram введите: `@BotFather`
- Это официальный бот Telegram для создания другихботов
- У него должна быть синяя галочка верификации

### Шаг 1.3: Создайте нового бота
- Напишите боту команду: `/newbot`
- BotFather попросит вас ввести имя бота (отображаемое имя)
  - Например: `Law Table Notifications`
- Затем BotFather попросит username бота (должен заканчиваться на `bot`)
  - Например: `lawtable_notify_bot`
  - Username должен быть уникальным

### Шаг 1.4: Сохраните Bot Token
- После создания бота BotFather отправит сообщение с токеном
- Токен выглядит примерно так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
- **ВАЖНО:** Никому не передавайте этот токен! Это ключ доступа к вашему боту

**Пример сообщения от BotFather:**
```
Done! Congratulations on your new bot. You will find it at t.me/lawtable_notify_bot.
You can now add a description, about section and profile picture for your bot,
see /help for a list of commands.

Use this token to access the HTTP API:
123456789:ABCdefGHIjklMNOpqrsTUVwxyz

Keep your token secure and store it safely, it can be used by anyone to control your bot.
```

---

## 2. Получение Bot Token

### Если вы потеряли токен:
1. Откройте чат с @BotFather
2. Напишите команду: `/mybots`
3. Выберите своего бота из списка
4. Нажмите "API Token"
5. BotFather пришлёт токен заново

### Где сохранить токен?
- Скопируйте токен в надёжное место (например, менеджер паролей)
- Он понадобится на следующем этапе настройки

---

## 3. Получение Chat ID для пользователей

Каждому пользователю системы нужен Chat ID, чтобы бот мог отправлять ему уведомления.

### Способ 1: Через @userinfobot (РЕКОМЕНДУЕТСЯ)

#### Шаг 3.1: Найдите @userinfobot
- В поиске Telegram введите: `@userinfobot`
- Откройте чат с этим ботом

#### Шаг 3.2: Получите Chat ID
- Просто напишите боту любое сообщение (например: "hi")
- Бот сразу ответит вашим Chat ID
- Chat ID выглядит как число: `123456789` или `987654321`

#### Шаг 3.3: Сохраните Chat ID
- Скопируйте это число
- Оно понадобится при добавлении пользователя в систему

### Способ 2: Через вашего бота

#### Шаг 3.2a: Найдите вашего бота
- В поиске Telegram найдите вашего бота по username (например: `@lawtable_notify_bot`)
- Откройте чат с ботом

#### Шаг 3.2b: Нажмите "Start"
- Нажмите кнопку `/start` или напишите команду `/start`
- Это активирует бота для общения с вами

#### Шаг 3.2c: Узнайте Chat ID через код
Вам нужно будет создать временную функцию в Google Apps Script:

```javascript
function getUpdates() {
  const botToken = '123456789:ABCdefGHIjklMNOpqrsTUVwxyz'; // Ваш токен
  const url = `https://api.telegram.org/bot${botToken}/getUpdates`;

  const response = UrlFetchApp.fetch(url);
  const data = JSON.parse(response.getContentText());

  Logger.log(JSON.stringify(data, null, 2));
}
```

Запустите эту функцию, и в логах вы увидите Chat ID всех, кто писал боту.

**ВАЖНО:** Каждый пользователь должен:
1. Найти вашего бота в Telegram
2. Нажать `/start`
3. Только после этого бот сможет отправлять ему сообщения

---

## 4. Настройка бота в системе

Теперь настроим бота в Google Sheets.

### Шаг 4.1: Откройте Google Sheets с Law Table
- Откройте вашу таблицу с судебными делами

### Шаг 4.2: Откройте меню настроек
- Нажмите на меню: `⚖️ Судебные дела` → `⚙️ Настройки` → `📱 Настройка Telegram`
- **ВАЖНО:** Это меню доступно только для роли **Администратор**

### Шаг 4.3: Введите Bot Token
- Появится диалоговое окно "Настройка Telegram Bot"
- Вставьте ваш Bot Token (полученный от @BotFather)
- Нажмите "OK"

### Шаг 4.4: Подтверждение
- Появится сообщение: "✅ Bot Token сохранён!"
- Токен сохранён в безопасном хранилище Properties Service
- Теперь система готова отправлять уведомления

---

## 5. Добавление пользователей и их Chat ID

### Шаг 5.1: Откройте управление пользователями
- Меню: `⚖️ Судебные дела` → `⚙️ Настройки` → `👥 Управление пользователями`

### Шаг 5.2: Добавьте нового пользователя
- Нажмите кнопку "➕ Добавить пользователя"

### Шаг 5.3: Заполните данные пользователя
Вам нужно ввести в JSON формате:

```json
{
  "email": "lawyer@example.com",
  "role": "LAWYER",
  "name": "Иванов Иван Иванович",
  "telegram_chat_id": "123456789",
  "notification_preferences": {
    "email": true,
    "telegram": true,
    "sms": false
  },
  "assigned_cases": ["А40-12345/2024"]
}
```

**Описание полей:**
- `email` - email пользователя (обязательно)
- `role` - роль: `ADMIN`, `MANAGER`, `LAWYER`, `ASSISTANT`, или `OBSERVER`
- `name` - полное имя пользователя
- `telegram_chat_id` - Chat ID из шага 3 (если есть)
- `notification_preferences.telegram` - включить Telegram уведомления (`true`/`false`)
- `assigned_cases` - массив номеров дел (для роли LAWYER)

### Шаг 5.4: Сохраните пользователя
- Нажмите "OK"
- Пользователь добавлен в систему

### Шаг 5.5: Повторите для каждого пользователя
- Добавьте всех пользователей, которым нужны уведомления
- Для каждого укажите их Chat ID (если они хотят получать Telegram уведомления)

---

## 6. Настройка типов уведомлений

Система поддерживает 4 типа уведомлений, которые отправляются разным ролям:

### Типы уведомлений:

#### 🔴 CRITICAL (Критические)
**Кто получает:** Admin, Manager
**Примеры:**
- Критические ошибки системы
- Просроченные дедлайны (сегодня)
- Важные системные события

#### 🟡 IMPORTANT (Важные)
**Кто получает:** Admin, Manager, Lawyer
**Примеры:**
- Приближающиеся дедлайны (завтра)
- Обновления по делам
- Изменения статусов

#### 🔵 INFO (Информационные)
**Кто получает:** Admin
**Примеры:**
- Статистика работы системы
- Успешное выполнение операций
- Логи производительности

#### 📊 DIGEST (Ежедневный дайджест)
**Кто получает:** Все роли (Admin, Manager, Lawyer, Assistant, Observer)
**Примеры:**
- Ежедневная сводка по всем делам
- Список приближающихся дедлайнов на неделю
- Общая статистика

### Настройка уведомлений для конкретной роли

Редактируйте пользователя через "Управление пользователями":

```json
{
  "email": "lawyer@example.com",
  "role": "LAWYER",
  "notification_preferences": {
    "email": true,
    "telegram": true,
    "sms": false
  }
}
```

Если `telegram: false`, пользователь **не будет** получать уведомления в Telegram (даже если у него указан Chat ID).

---

## 7. Настройка ежедневного дайджеста

### Шаг 7.1: Откройте настройки системы
- Меню: `⚖️ Судебные дела` → `⚙️ Настройки` → `Настройки системы`

### Шаг 7.2: Найдите параметр `NOTIFICATIONS.DIGEST_TIME`
- По умолчанию: `09:00` (9 утра)
- Измените на удобное время, например: `08:30` или `10:00`

### Шаг 7.3: Включите отправку дайджеста
- Найдите параметр `NOTIFICATIONS.SEND_DAILY_DIGEST`
- Установите значение: `true`

### Шаг 7.4: Настройте триггер для дайджеста
Вариант 1: Через меню (рекомендуется)
- Меню: `⚖️ Судебные дела` → `⚙️ Настройки` → `⏰ Настроить триггеры`
- Выберите "Настроить Telegram дайджест"

Вариант 2: Через код
Откройте Apps Script и выполните функцию:
```javascript
TelegramNotifier.setupDailyDigest();
```

### Шаг 7.5: Проверьте триггер
1. Откройте Apps Script (Расширения → Apps Script)
2. Слева нажмите на значок "⏰" (Триггеры)
3. Должен быть триггер:
   - Функция: `TelegramNotifier.sendDailyDigest`
   - Тип события: По времени
   - Интервал: Каждый день в указанное время

---

## 8. Тестирование уведомлений

### Тест 1: Отправка тестового сообщения

Откройте Apps Script и выполните:

```javascript
function testTelegramNotification() {
  TelegramNotifier.sendInfo(
    'Тестовое уведомление',
    'Это тестовое сообщение из Law Table. Если вы его получили, всё работает! ✅'
  );
}
```

**Ожидаемый результат:**
- Все пользователи с ролью `ADMIN` и включённым `telegram: true` должны получить сообщение

### Тест 2: Критическое уведомление

```javascript
function testCriticalNotification() {
  TelegramNotifier.sendCritical(
    'ТЕСТ: Критическое уведомление',
    'Тест отправки критического уведомления администраторам и менеджерам.',
    'А40-12345/2024'
  );
}
```

**Ожидаемый результат:**
- Admin и Manager должны получить сообщение с 🔴

### Тест 3: Уведомление о дедлайне

```javascript
function testDeadlineNotification() {
  TelegramNotifier.notifyDeadline(
    'А40-12345/2024',
    'Судебное заседание',
    '15.11.2024',
    1 // Завтра
  );
}
```

**Ожидаемый результат:**
- Admin, Manager и Lawyer (если дело назначено) должны получить уведомление

### Тест 4: Ежедневный дайджест

```javascript
function testDailyDigest() {
  TelegramNotifier.sendDailyDigest();
}
```

**Ожидаемый результат:**
- Все пользователи (с включённым Telegram) должны получить дайджест

---

## 9. Устранение неполадок

### Проблема 1: "Bot token не настроен"

**Причина:** Токен не сохранён в системе
**Решение:**
1. Меню → Настройки → Настройка Telegram
2. Введите Bot Token заново
3. Проверьте, что токен скопирован полностью (без пробелов в начале/конце)

### Проблема 2: Уведомления не приходят конкретному пользователю

**Возможные причины:**

#### Причина A: Пользователь не написал боту `/start`
**Решение:**
1. Пользователь должен найти бота в Telegram
2. Открыть чат с ботом
3. Нажать кнопку "Start" или написать `/start`
4. После этого попробуйте отправить уведомление снова

#### Причина B: Неправильный Chat ID
**Решение:**
1. Попросите пользователя получить Chat ID через @userinfobot
2. Убедитесь, что Chat ID - это число (без букв)
3. Обновите Chat ID пользователя через "Управление пользователями"

#### Причина C: Telegram уведомления отключены
**Решение:**
1. Откройте "Управление пользователями"
2. Найдите пользователя
3. Проверьте, что `notification_preferences.telegram: true`

#### Причина D: Роль не подходит для этого типа уведомления
**Решение:**
- Проверьте таблицу соответствия ролей и типов уведомлений (раздел 6)
- Например, роль Observer получает только дайджест
- Измените роль пользователя, если нужно

### Проблема 3: Ошибка "Chat not found"

**Причина:** Бот заблокирован пользователем или пользователь не нажал `/start`
**Решение:**
1. Пользователь должен разблокировать бота (если блокировал)
2. Написать боту `/start`
3. Попробуйте отправить уведомление снова

### Проблема 4: Дайджест не отправляется автоматически

**Возможные причины:**

#### Причина A: Триггер не настроен
**Решение:**
1. Расширения → Apps Script
2. Слева значок "⏰" (Триггеры)
3. Если триггера `TelegramNotifier.sendDailyDigest` нет:
   - Выполните функцию `TelegramNotifier.setupDailyDigest()`

#### Причина B: Триггер отключён
**Решение:**
1. Проверьте список триггеров
2. Убедитесь, что триггер активен (не зачёркнут)
3. Если отключён - удалите и создайте заново

#### Причина C: Превышен лимит выполнения
**Решение:**
- Google Apps Script имеет дневной лимит выполнения
- Проверьте логи: меню → Логи → Показать статистику
- Оптимизируйте количество уведомлений

### Проблема 5: Ошибка "Invalid token"

**Причина:** Неправильный Bot Token
**Решение:**
1. Откройте чат с @BotFather
2. Команда: `/mybots` → выберите бота → "API Token"
3. Скопируйте новый токен (убедитесь, что копируете полностью)
4. Меню → Настройки → Настройка Telegram
5. Введите токен заново

### Проблема 6: Слишком длинное сообщение

**Причина:** Telegram ограничивает длину сообщений до 4096 символов
**Решение:**
- Система автоматически обрезает длинные дайджесты
- Если проблема в вашем коде - разбейте сообщение на части

---

## Проверочный чек-лист

Перед использованием системы убедитесь:

- [ ] Создан Telegram бот через @BotFather
- [ ] Получен и сохранён Bot Token
- [ ] Bot Token добавлен в систему (меню → Настройка Telegram)
- [ ] Каждый пользователь получил свой Chat ID
- [ ] Каждый пользователь написал боту `/start`
- [ ] Пользователи добавлены через "Управление пользователями"
- [ ] У пользователей указаны Chat ID
- [ ] У пользователей включено `telegram: true` в настройках
- [ ] Настроен триггер для ежедневного дайджеста (если нужен)
- [ ] Протестированы уведомления (раздел 8)
- [ ] Проверены логи на наличие ошибок

---

## Дополнительные возможности

### Кастомные уведомления

Вы можете создавать собственные уведомления:

```javascript
function customNotification() {
  const text = '📢 <b>Важное объявление</b>\n\nТекст вашего объявления здесь.';
  TelegramNotifier.sendNotification('important', text);
}
```

### HTML форматирование

Telegram поддерживает HTML теги:
- `<b>жирный</b>` → **жирный**
- `<i>курсив</i>` → *курсив*
- `<code>код</code>` → `код`
- `<pre>блок кода</pre>` → ```блок кода```
- `<a href="URL">ссылка</a>` → [ссылка](URL)

Пример:
```javascript
const message =
  `🔴 <b>Критическое уведомление</b>\n\n` +
  `📋 Дело: <code>А40-12345/2024</code>\n` +
  `📅 Дата: <i>15.11.2024</i>\n` +
  `🔗 <a href="https://kad.arbitr.ru">Открыть КАД</a>`;
```

---

## Безопасность

### Хранение Bot Token
- Токен хранится в Properties Service (зашифрованное хранилище Google)
- Токен недоступен через UI таблицы
- Только администраторы могут изменить токен

### Рекомендации:
1. **НЕ** публикуйте Bot Token в коде или документации
2. **НЕ** делитесь токеном с посторонними
3. Если токен скомпрометирован - создайте нового бота
4. Ограничьте доступ к Google Sheets только нужным сотрудникам
5. Регулярно проверяйте список пользователей системы

---

## Поддержка

Если у вас возникли проблемы:

1. **Проверьте логи:**
   - Меню → Логи и мониторинг → Показать статистику логов
   - Меню → Логи и мониторинг → Поиск в логах

2. **Проверьте конфигурацию:**
   - Убедитесь, что Bot Token введён правильно
   - Проверьте настройки пользователей
   - Убедитесь, что триггеры настроены

3. **Тестируйте поэтапно:**
   - Сначала протестируйте простое уведомление (Тест 1)
   - Затем тестируйте более сложные сценарии

4. **Обратитесь к документации:**
   - [Telegram Bot API](https://core.telegram.org/bots/api)
   - [Google Apps Script](https://developers.google.com/apps-script)

---

**Версия инструкции:** 1.0
**Дата обновления:** 14.11.2024
**Система:** Law Table v2.1
