# 🚀 БЫСТРЫЙ СТАРТ - Автоматическая миграция

## ⚡ ЗА 3 ШАГА К УСПЕХУ

### ШАГ 1: Откройте Apps Script (30 сек)
1. Откройте вашу Google Sheets таблицу
2. **Расширения** → **Apps Script**
3. Откроется редактор

### ШАГ 2: Добавьте 2 файла (2 минуты)

#### Файл 1: ColumnsConfig
1. Нажмите **+** рядом с "Файлы" → **Скрипт**
2. Назовите: `ColumnsConfig`
3. Скопируйте код из `ColumnsConfig.gs`
4. Вставьте и сохраните (Ctrl+S)

#### Файл 2: Migration
1. Нажмите **+** рядом с "Файлы" → **Скрипт**
2. Назовите: `Migration`
3. Скопируйте код из `Migration.gs`
4. Вставьте и сохраните (Ctrl+S)

### ШАГ 3: Запустите миграцию (5 минут)

1. В редакторе выберите функцию: **`migrateAllScripts`**
2. Нажмите **▶ Запустить**
3. Разрешите доступ (если попросит)
4. Смотрите лог - там будут ТОЧНЫЕ инструкции!

---

## 📋 ЧТО ДЕЛАТЬ С ЛОГОМ

После запуска `migrateAllScripts()` в логе появятся инструкции типа:

```
📝 ШАГ 1: ИСТЕЦ (Plaintiff) - Колонка H
   НАЙТИ (Regex): row\[6\]
   ЗАМЕНИТЬ НА: row[COLUMNS.PLAINTIFF]
```

### Как использовать:

1. **Нажмите Ctrl+H** (поиск и замена)
2. **Включите "Regex"** (галочка в окне замены)
3. **Скопируйте из лога** что искать
4. **Вставьте** что заменить
5. **Нажмите "Заменить все"**
6. **Повторите** для каждого шага

---

## ✅ ПРОВЕРКА

После всех замен:
1. Выберите функцию: **`verifyMigration`**
2. Нажмите **▶ Запустить**
3. Смотрите результат в логе

Если всё ✅ - миграция успешна!

---

## 🔥 ЭКСТРЕННАЯ ПОМОЩЬ

### Проблема: "COLUMNS is not defined"
**Решение:** Добавьте файл `ColumnsConfig.gs` (см. Шаг 2.1)

### Проблема: "Cannot read property..."
**Решение:** Проверьте что заменили ВСЕ старые индексы

### Проблема: "Wrong values in cells"
**Решение:** Запустите `showCurrentStructure()` - посмотрите что не так

---

## 📞 НУЖНА ПОМОЩЬ?

Отправьте мне:
1. Скриншот лога после `migrateAllScripts()`
2. Код ваших существующих скриптов
3. Скриншот первой строки таблицы (заголовки)

Я помогу!

---

## 🎯 БЫСТРЫЕ КОМАНДЫ

Запускайте эти функции для разных задач:

| Функция | Что делает |
|---------|------------|
| `migrateAllScripts()` | Показывает инструкции по миграции |
| `verifyMigration()` | Проверяет что всё работает |
| `showCurrentStructure()` | Показывает структуру таблицы |
| `createBackup()` | Напоминает сделать backup |

---

## 💡 ПОЛЕЗНЫЕ СОВЕТЫ

1. **Сделайте backup** перед изменениями
2. **Используйте Regex** в поиске и замене
3. **Проверяйте каждую замену** - row[3] может быть статусом ИЛИ инстанцией
4. **Тестируйте** после миграции
5. **Не паникуйте** - всегда можно восстановить из backup

---

🎉 **ГОТОВО! Теперь ваши скрипты используют правильные индексы!**
