# 🍎 Инструкция для вашего MacBook

## ✅ Что уже сделано

Я создал **полную интеграцию Google Apps Script с GitHub**! 🎉

Вот что готово:
- ✅ Структура проекта с clasp
- ✅ GitHub Actions workflows для CI/CD
- ✅ ESLint и Prettier для качества кода
- ✅ Примеры кода (Code.gs, Utils.gs)
- ✅ Полная документация
- ✅ Setup скрипты для автоматизации

---

## 📦 Что нужно сделать на вашем Mac

### Шаг 1: Скопировать проект на локальную машину

Проект находится по пути: `/home/user/legal-ai-sheets-scripts`

Вам нужно **скопировать эту папку** на ваш MacBook. Есть несколько способов:

#### Способ 1: Скачать архив

Архив создан: `/home/user/legal-ai-sheets-scripts.tar.gz` (52KB)

На вашем Mac:
```bash
# Создайте директорию для проектов
mkdir -p ~/Projects
cd ~/Projects

# Распакуйте архив (после того как скопируете его)
tar -xzf ~/Downloads/legal-ai-sheets-scripts.tar.gz
cd legal-ai-sheets-scripts
```

#### Способ 2: Пересоздать структуру вручную

Можно просто пересоздать структуру на вашем Mac и скопировать файлы один за другим.

---

### Шаг 2: Создать репозиторий на GitHub

#### Вариант A: Через веб-интерфейс (проще)

1. Откройте https://github.com/new
2. Создайте репозиторий:
   - **Name:** `legal-ai-sheets-scripts`
   - **Description:** "Google Apps Script для автоматизации работы с таблицами Legal AI"
   - **Public** или **Private**
   - ✅ Снимите галочку "Add a README file" (у нас уже есть)
3. Нажмите **Create repository**
4. **Скопируйте URL репозитория** (будет показан на следующей странице)

#### Вариант B: Через GitHub CLI

```bash
# Если у вас установлен gh
gh auth login
gh repo create legal-ai-sheets-scripts --public
```

---

### Шаг 3: На вашем MacBook

#### 3.1. Перейдите в директорию проекта

```bash
cd ~/Projects/legal-ai-sheets-scripts
```

#### 3.2. Настройте Git remote

```bash
# Добавьте remote (замените URL на ваш!)
git remote add origin https://github.com/Andrew821667/legal-ai-sheets-scripts.git

# Проверьте
git remote -v
```

#### 3.3. Отправьте на GitHub

```bash
# Отправьте код
git push -u origin main
```

---

### Шаг 4: Настройте GitHub Secrets

Это нужно для автоматического деплоя!

#### 4.1. Получите credentials

На вашем Mac (у вас уже есть этот файл):

```bash
cat ~/.clasprc.json
```

**Скопируйте ВСЁ содержимое!** Должен быть JSON примерно такой:

```json
{
  "tokens": {
    "default": {
      "client_id": "YOUR_CLIENT_ID.apps.googleusercontent.com",
      "client_secret": "YOUR_CLIENT_SECRET",
      "type": "authorized_user",
      "refresh_token": "YOUR_REFRESH_TOKEN",
      "access_token": "YOUR_ACCESS_TOKEN"
    }
  }
}
```

#### 4.2. Добавьте в GitHub

1. Откройте ваш репозиторий: https://github.com/Andrew821667/legal-ai-sheets-scripts
2. Перейдите в **Settings**
3. В боковом меню: **Secrets and variables** → **Actions**
4. Нажмите **New repository secret**
5. Создайте секрет:
   - **Name:** `CLASPRC_JSON`
   - **Value:** *вставьте весь JSON из предыдущего шага*
6. Нажмите **Add secret**

---

### Шаг 5: Установите зависимости и настройте проект

```bash
# Перейдите в директорию проекта
cd ~/Projects/legal-ai-sheets-scripts

# Установите зависимости
npm install

# Запустите автоматическую настройку
chmod +x scripts/setup-local.sh
./scripts/setup-local.sh
```

Скрипт setup автоматически:
- ✅ Проверит Node.js и npm
- ✅ Установит clasp (если нужно)
- ✅ Проверит авторизацию (у вас уже есть)
- ✅ Предложит скачать код из Google Sheets

---

### Шаг 6: Скачайте текущий код из Google Sheets

```bash
npm run pull
```

Это скачает **текущие скрипты** из вашей таблицы в папку `src/`.

---

### Шаг 7: Проверьте что всё работает

#### 7.1. Локальная проверка

```bash
# Проверить качество кода
npm run lint

# Открыть скрипт в браузере
npm run open

# Загрузить изменения обратно в Google Sheets
npm run push
```

#### 7.2. Проверка CI/CD

```bash
# Сделайте тестовое изменение
echo "# Test CI/CD" >> test.txt
git add test.txt
git commit -m "test: проверка автоматического деплоя"
git push origin main
```

Теперь откройте GitHub → **Actions** и посмотрите как работает автодеплой! 🎉

---

## 🎯 Что получилось

### ✅ Локальная разработка

- Редактируете код в `src/`
- Запускаете `npm run lint` для проверки
- Загружаете в Google Sheets: `npm run push`
- Тестируете в таблице
- Коммитите и пушите в GitHub

### ✅ Автоматический деплой

При каждом push в `main`:
1. GitHub Actions запускается автоматически
2. Код проверяется (lint)
3. Загружается в Google Apps Script
4. Создается новая версия
5. Деплоится на production

### ✅ Версионирование

- Вся история изменений в Git
- Можно откатиться на любую версию
- Code review через Pull Requests
- Все изменения отслеживаются

---

## 📁 Структура проекта

```
legal-ai-sheets-scripts/
├── .github/workflows/       # CI/CD pipelines
│   ├── deploy.yml          # Автодеплой
│   └── test.yml            # Тестирование
├── src/                    # Ваш код
│   ├── Code.gs             # Главные функции
│   ├── Utils.gs            # Утилиты
│   └── appsscript.json     # Манифест
├── scripts/
│   └── setup-local.sh      # Автонастройка
├── .clasp.json             # Script ID
├── package.json            # npm конфигурация
├── README.md               # Полная документация
├── QUICKSTART.md           # Быстрый старт
└── SETUP_GITHUB.md         # Настройка GitHub
```

---

## 🎨 Полезные команды

### Основные

```bash
npm run pull          # Скачать из Google Sheets
npm run push          # Загрузить в Google Sheets
npm run watch         # Автозагрузка при изменениях
npm run open          # Открыть в браузере
npm run logs          # Показать логи
```

### Качество кода

```bash
npm run lint          # Проверить код
npm run lint:fix      # Автоисправление
npm run format        # Форматировать код
```

---

## 📖 Документация

- **README.md** - Полная документация проекта
- **QUICKSTART.md** - Быстрый старт за 5 минут
- **SETUP_GITHUB.md** - Детальная настройка GitHub и CI/CD
- **CHANGELOG.md** - История изменений

---

## 🐛 Если что-то не работает

### Ошибка: "clasp not found"

```bash
sudo npm install -g @google/clasp
```

### Ошибка: "You are not logged in"

```bash
clasp login
```

### Ошибка: "Permission denied"

Проверьте права доступа к таблице и скрипту в Google Sheets.

### CI/CD не работает

1. Проверьте GitHub Secrets
2. Убедитесь что `CLASPRC_JSON` содержит правильный JSON
3. Проверьте логи в GitHub Actions

---

## ✨ Следующие шаги

1. **Скачайте текущий код:**
   ```bash
   npm run pull
   ```

2. **Изучите структуру кода** в `src/`

3. **Внесите изменения** и протестируйте:
   ```bash
   npm run lint
   npm run push
   ```

4. **Закоммитьте и запустите автодеплой:**
   ```bash
   git add .
   git commit -m "feat: новая функция"
   git push origin main
   ```

5. **Наслаждайтесь автоматизацией!** 🎉

---

## 🆘 Нужна помощь?

Если возникнут вопросы:
1. Проверьте README.md
2. Посмотрите SETUP_GITHUB.md
3. Изучите примеры в `src/`
4. Создайте Issue на GitHub

---

**Удачи с интеграцией!** 🚀

P.S. Не забудьте добавить `CLASPRC_JSON` в GitHub Secrets - без этого автодеплой не заработает!
