# 🚀 Деплой Telegram Bot на Vercel

## Новая архитектура v2.0

**Telegram → Vercel → Google Sheets API** (БЕЗ Apps Script!)

---

## ✨ Преимущества

✅ **Прямой доступ к Google Sheets** - через API v4 или CSV export
✅ **Автодеплой** - push в GitHub → автоматический деплой
✅ **Быстрая работа** - Vercel edge network
✅ **Надежность** - не зависит от Apps Script
✅ **Бесплатно** - 100GB трафика/месяц

---

## 📋 Требования

- GitHub аккаунт
- Vercel аккаунт (бесплатный)
- Telegram Bot Token
- Google Sheets с доступом "Anyone with link"
- Google API Key (опционально, но рекомендуется)

---

## 🎯 Быстрая настройка (5 минут)

### Шаг 1: Подключить проект к Vercel

1. Зайди на https://vercel.com/login
2. Войди через GitHub
3. Нажми **"Add New Project"**
4. Выбери репозиторий `Andrew821667/Law_table`
5. Vercel автоматически обнаружит настройки из `vercel.json`
6. Нажми **"Deploy"**

### Шаг 2: Настроить переменные окружения

В Vercel dashboard:

1. Открой **Settings → Environment Variables**
2. Добавь три переменные:

```
TELEGRAM_BOT_TOKEN = 8454450852:AAGHlplAsdMuO53OHf5puMRSUpeCoUhinAM
GOOGLE_API_KEY = AIzaSyA157k12RMUz_UIbhDyuPjdj__sWpSGBZQ
SHEET_NAME = Судебные дела
```

3. Для каждой выбери **Production, Preview, Development**
4. Нажми **"Save"**

### Шаг 3: Redeploy

После добавления переменных:

1. Перейди в **Deployments**
2. Нажми на последний деплой
3. Нажми **"Redeploy"**

Получишь URL: `https://law-table-xyz.vercel.app`

### Шаг 4: Настроить Telegram Webhook

Замени `YOUR-VERCEL-URL` на свой URL:

```bash
curl "https://api.telegram.org/bot8454450852:AAGHlplAsdMuO53OHf5puMRSUpeCoUhinAM/setWebhook?url=https://YOUR-VERCEL-URL.vercel.app/api/webhook"
```

Ответ должен быть:
```json
{"ok":true,"result":true,"description":"Webhook was set"}
```

---

## ✅ Проверка работы

1. Отправь `/start` боту в Telegram
2. Должна появиться кнопка "📅 Мои предстоящие заседания"
3. Нажми и проверь, что данные загружаются

---

## 🔄 Автодеплой через GitHub Actions

### Настройка GitHub Secrets

Чтобы включить автодеплой при каждом push:

1. Зайди в **Settings → Secrets and variables → Actions**
2. Добавь три секрета:

**VERCEL_TOKEN**
- Открой https://vercel.com/account/tokens
- Создай новый токен
- Скопируй и добавь в GitHub

**VERCEL_ORG_ID**
- Открой Vercel dashboard
- Перейди в Settings проекта
- Скопируй "Organization ID"

**VERCEL_PROJECT_ID**
- В том же разделе Settings
- Скопируй "Project ID"

3. После настройки - каждый push в `main` или `claude/**` будет автоматически деплоиться!

---

## 🛠️ Локальная разработка

```bash
# Установи зависимости
npm install

# Запусти локально
npm run dev

# Откроется на http://localhost:3000
```

Для тестирования webhook локально используй ngrok:

```bash
ngrok http 3000
# Скопируй URL и настрой webhook
```

---

## 📊 Структура проекта

```
Law_table/
├── api/
│   ├── webhook.js       # Главный обработчик Telegram
│   └── test.js          # Тестовый endpoint
├── vercel.json          # Конфигурация Vercel
├── package.json         # Зависимости
└── .github/
    └── workflows/
        └── vercel-deploy.yml  # Автодеплой
```

---

## 🔍 Мониторинг и отладка

### Просмотр логов

В Vercel dashboard:
1. Перейди в **Deployments**
2. Выбери последний деплой
3. Нажми **"View Function Logs"**

### Проверка webhook

```bash
curl "https://api.telegram.org/bot8454450852:AAGHlplAsdMuO53OHf5puMRSUpeCoUhinAM/getWebhookInfo"
```

Должен показывать:
```json
{
  "url": "https://your-vercel-url.vercel.app/api/webhook",
  "has_custom_certificate": false,
  "pending_update_count": 0
}
```

### Тестовый endpoint

Проверь что Vercel работает:
```bash
curl https://YOUR-VERCEL-URL.vercel.app/api/test
```

Ответ:
```json
{
  "ok": true,
  "message": "Vercel works!",
  "timestamp": "2025-11-19T10:30:00.000Z"
}
```

---

## 🔐 Безопасность

### Google API Key

Текущий ключ в коде - временный. Для production:

1. Создай свой ключ: https://console.cloud.google.com/apis/credentials
2. Ограничь его только Google Sheets API
3. Добавь ограничения по домену (Vercel)
4. Замени в переменных окружения Vercel

### Hardcoded значения

В `api/webhook.js` есть:
```javascript
const GOOGLE_API_KEY = process.env.GOOGLE_API_KEY || 'AIzaSy...';
```

Fallback ключ будет работать, но **лучше установить свой** через Vercel env vars!

---

## 🚨 Устранение проблем

### Бот не отвечает

1. Проверь webhook: `getWebhookInfo`
2. Проверь логи в Vercel
3. Проверь переменные окружения

### "Таблица недоступна"

1. Убедись что таблица: **Anyone with link can view**
2. Проверь `GOOGLE_API_KEY` в Vercel
3. Попробуй открыть таблицу в браузере

### 403 при деплое

1. Проверь `VERCEL_TOKEN` в GitHub Secrets
2. Убедись что токен действителен
3. Проверь права на проект в Vercel

---

## 📈 Дальнейшие улучшения

- [ ] Добавить кэширование данных из Sheets
- [ ] Реализовать rate limiting
- [ ] Добавить больше команд бота
- [ ] Настроить алерты через Vercel
- [ ] Добавить мониторинг (Sentry/LogRocket)

---

## 🆘 Поддержка

Проблемы с деплоем? Создай issue на GitHub!

**Vercel = Надежность + Простота + Бесплатно** 🚀
