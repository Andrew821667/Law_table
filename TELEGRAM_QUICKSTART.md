# 🚀 Быстрая настройка Telegram бота (пошагово)

> **Время:** 10-15 минут
> **Сложность:** Легко
> **Требования:** Telegram аккаунт, доступ к Google Sheets

---

## ✅ ШАГ 0: Удаление старых настроек

**Если настраиваете бота ВПЕРВЫЕ - пропустите этот шаг!**

Если были попытки настроить бота раньше, сначала очистите старые настройки:

1. Откройте Google Sheets с таблицей Law_table
2. Откройте Apps Script: **Расширения → Apps Script**
3. Вставьте в консоль и запустите:
   ```javascript
   TelegramCleanup.showStatus();  // Посмотреть что есть
   TelegramCleanup.fullCleanup(); // Удалить всё старое
   ```
4. Подтвердите удаление

✅ **Готово! Теперь можно настраивать заново.**

---

## 📱 ШАГ 1: Создание бота в Telegram (5 минут)

### 1.1. Откройте Telegram
- На телефоне или компьютере

### 1.2. Найдите @BotFather
- В поиске введите: `@BotFather`
- Откройте чат (с синей галочкой)

### 1.3. Создайте бота
Напишите команды по очереди:

```
/newbot
```

**Придумайте имя бота:**
Пример: `Law Table Bot`

**Придумайте username:**
Пример: `lawtable_bot` (должен заканчиваться на `_bot`)

### 1.4. Сохраните Bot Token
BotFather пришлёт токен вида:
`123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

**📋 СКОПИРУЙТЕ ЕГО - он понадобится на следующем шаге!**

✅ **Бот создан!**

---

## 🔧 ШАГ 2: Настройка бота в Google Sheets (3 минуты)

### 2.1. Откройте таблицу Law_table

### 2.2. Откройте Apps Script
- Меню: **Расширения → Apps Script**

### 2.3. Вставьте и запустите команду
В консоли вставьте:

```javascript
TelegramNotifier.setup();
```

Нажмите **▶ Run**

### 2.4. Введите Bot Token
- Появится окно "Настройка Telegram Bot"
- Вставьте токен из шага 1.4
- Нажмите **OK**

✅ **Bot Token сохранён!**

---

## 👤 ШАГ 3: Получение Chat ID (2 минуты на каждого)

Каждому пользователю, который хочет получать уведомления:

### Вариант А: Через @userinfobot (ПРОЩЕ)

1. Откройте Telegram
2. Найдите `@userinfobot`
3. Напишите боту любое сообщение
4. Бот ответит вашим **Chat ID** (например: `123456789`)
5. **📋 СОХРАНИТЕ ЭТО ЧИСЛО!**

### Вариант Б: Через вашего бота

1. Найдите вашего бота по username (например: `@lawtable_bot`)
2. Напишите боту `/start`
3. Выполните в Apps Script:
   ```javascript
   function getUpdates() {
     const botToken = 'ВАШ_BOT_TOKEN'; // Вставьте токен
     const url = `https://api.telegram.org/bot${botToken}/getUpdates`;
     const response = UrlFetchApp.fetch(url);
     Logger.log(response.getContentText());
   }
   ```
4. В логах найдите `"chat":{"id":123456789}` - это Chat ID

✅ **Chat ID получен!**

---

## 👥 ШАГ 4: Добавление пользователей (5 минут)

### 4.1. Откройте Apps Script

### 4.2. Добавьте пользователя

Вставьте и запустите (замените на свои данные):

```javascript
function addTelegramUser() {
  const userData = {
    email: "ваш@email.com",           // ← ВАШ EMAIL
    role: "ADMIN",                     // ← РОЛЬ (ADMIN/LAWYER/MANAGER)
    name: "Иван Иванов",              // ← ВАШЕ ИМЯ
    telegram_chat_id: "123456789",    // ← ВАШ CHAT ID из шага 3
    notification_preferences: {
      email: true,
      telegram: true,                  // ← ВКЛЮЧИТЬ TELEGRAM
      sms: false
    },
    assigned_cases: []                 // ← Дела (для LAWYER)
  };

  UserManager.addUser(userData.email, userData);
  Logger.log('✅ Пользователь добавлен!');
}
```

### 4.3. Запустите функцию
- Нажмите **▶ Run** на функции `addTelegramUser`

### 4.4. Повторите для других пользователей
- Измените данные и запустите снова

✅ **Пользователи добавлены!**

---

## 🧪 ШАГ 5: Тестирование (2 минуты)

### 5.1. Отправьте тестовое сообщение

Вставьте и запустите:

```javascript
function testTelegram() {
  TelegramNotifier.sendInfo(
    'Тестовое сообщение',
    'Если вы это видите - всё работает! ✅'
  );
}
```

### 5.2. Проверьте Telegram
- Вы должны получить сообщение от бота
- Если получили - **ВСЁ РАБОТАЕТ!** 🎉

### 5.3. Если не получили
Проверьте:
- [ ] Вы написали боту `/start`? (обязательно!)
- [ ] Chat ID правильный?
- [ ] В UserManager у вас `telegram: true`?
- [ ] Bot Token правильный?

---

## 🎯 ШАГ 6: Настройка ежедневного дайджеста (опционально)

Если хотите получать ежедневную сводку:

### 6.1. Запустите настройку

```javascript
TelegramNotifier.setupDailyDigest();
```

### 6.2. Проверьте триггер
1. Apps Script → слева значок **⏰** (Триггеры)
2. Должен быть триггер `TelegramNotifier.sendDailyDigest`
3. Время по умолчанию: 09:00

### 6.3. Протестируйте дайджест

```javascript
TelegramNotifier.sendDailyDigest();
```

Все пользователи должны получить сводку!

✅ **Дайджест настроен!**

---

## 🎉 ГОТОВО!

Теперь ваш Telegram бот готов к работе!

### Что дальше?

1. **Добавьте остальных пользователей** (повторите Шаг 4)
2. **Настройте типы уведомлений** (см. TELEGRAM_SETUP.md)
3. **Интегрируйте с другими модулями** (DeadlineChecker, CaseManager)

### Типы уведомлений:

- `TelegramNotifier.sendCritical()` → Admin, Manager (🔴)
- `TelegramNotifier.sendImportant()` → Admin, Manager, Lawyer (🟡)
- `TelegramNotifier.sendInfo()` → Admin (🔵)
- `TelegramNotifier.sendDailyDigest()` → Все (📊)

---

## 🆘 Помощь

Если что-то не работает:

1. **Проверьте статус:**
   ```javascript
   TelegramCleanup.showStatus();
   ```

2. **Посмотрите логи:**
   ```javascript
   AppLogger.showStats();
   ```

3. **Полная документация:**
   - См. `TELEGRAM_SETUP.md` для подробностей

---

**Версия:** 1.0
**Дата:** 2024-11-17
**Время настройки:** ~15 минут
