# Исправление ошибки "302 Moved Temporarily"

## 🔴 Проблема

Telegram Bot не отвечает на команды. Диагностика показывает:

```
⚠️ Последняя ошибка: Wrong response from the webhook: 302 Moved Temporarily
• Ожидает обновлений: 8
```

**Причина:** Развёртывание веб-приложения Apps Script возвращает редирект `302` вместо нормального ответа `200 OK`. Telegram не может обработать такой ответ.

---

## ✅ Решение: Создать НОВОЕ развёртывание

**ВАЖНО:** Нужно создать **НОВОЕ** развёртывание, а не обновлять старое!

---

## 📋 Пошаговая инструкция (Русский интерфейс)

### Шаг 1: Открыть Apps Script

1. Откройте таблицу Law Table
2. Меню: **Расширения** → **Apps Script**

---

### Шаг 2: Удалить старое развёртывание (опционально)

1. В правом верхнем углу нажмите: **"Разверните"** → **"Управление развёртываниями"**
2. Найдите старое развёртывание с типом "Веб-приложение"
3. Нажмите на значок **⋮** (три точки справа)
4. Выберите: **"Удалить развёртывание"**
5. Подтвердите удаление

---

### Шаг 3: Создать НОВОЕ развёртывание

1. В правом верхнем углу нажмите: **"Разверните"** → **"Новое развёртывание"**

2. **Выберите тип развёртывания:**
   - Нажмите на иконку ⚙️ слева (**"Выбрать тип"**)
   - Выберите: **"Веб-приложение"**

3. **Заполните поля:**
   ```
   • Новое описание: Law Table Telegram Bot
   • Выполнять как: Я (ваш email)
   • У кого есть доступ: Все ⬅️ КРИТИЧЕСКИ ВАЖНО!
   ```

   **⚠️ ВНИМАНИЕ:** Если выбрать "Только я", webhook не будет работать!

4. Нажмите: **"Развернуть"**

---

### Шаг 4: Предоставить разрешения

Если система запросит разрешения:

1. Выберите ваш Google аккаунт
2. Нажмите: **"Дополнительно"** (внизу слева)
3. Нажмите: **"Перейти на страницу Law Table (небезопасно)"**
4. Прокрутите вниз
5. Нажмите: **"Разрешить"**

---

### Шаг 5: Скопировать URL веб-приложения

1. После успешного развёртывания появится окно с информацией
2. **СКОПИРУЙТЕ** строку **"URL веб-приложения:"**
   - URL выглядит так: `https://script.google.com/macros/s/AKfycby.../exec`
3. Сохраните URL - он понадобится на следующем шаге

---

### Шаг 6: Обновить Webhook в Law Table

1. Вернитесь в таблицу Law Table
2. Меню: **⚖️ Судебные дела** → **⚙️ Настройки** → **🔔 Уведомления** → **🤖 Telegram Bot** → **🔗 Настроить Webhook**
3. Вставьте **новый URL** из Шага 5
4. Нажмите **OK**

Вы увидите сообщение:
```
✅ Webhook успешно настроен!
Webhook URL: https://script.google.com/macros/s/...
Бот готов принимать команды!
```

---

### Шаг 7: Проверить работу бота

1. Откройте Telegram
2. Найдите вашего бота: **@lawtable_notify_bot**
3. Отправьте команду: `/start`
4. Бот должен ответить приветственным сообщением!

Если бот ответил - webhook работает! ✅

---

### Шаг 8: Привязать аккаунт

1. В Law Table создайте код привязки:
   - Меню → Настройки → Telegram Bot → Создать код привязки
2. Скопируйте код (например: `639E3459`)
3. Отправьте боту: `/link 639E3459`
4. Бот ответит: **"✅ Аккаунт успешно привязан!"**

---

## 🔍 Диагностика после исправления

После выполнения всех шагов проверьте статус:

**Меню → Настройки → Telegram Bot → Диагностика бота**

Должно быть:
```
✅ Bot Token: ...
✅ Webhook URL: https://script.google.com/macros/s/.../exec
✅ Бот активен: @lawtable_notify_bot
✅ Webhook зарегистрирован в Telegram
   • Ожидает обновлений: 0 ⬅️ ДОЛЖНО БЫТЬ 0!
   • БЕЗ ошибок ⬅️ НЕТ строки "Последняя ошибка"
```

---

## ❓ Частые проблемы

### Проблема: "Ожидает обновлений: 8" не обнуляется

**Решение:** Обновления обработаются автоматически, когда бот начнёт отвечать. Попробуйте отправить `/start` боту.

---

### Проблема: Бот всё ещё молчит после создания нового развёртывания

**Проверьте:**
1. Доступ установлен на **"Все"** (не "Только я")
2. URL скопирован **полностью** (заканчивается на `/exec`)
3. Webhook URL в Law Table **совпадает** с URL из Apps Script
4. Развёртывание **активно** (зелёная точка в списке развёртываний)

---

### Проблема: Ошибка при создании развёртывания

**Решение:**
1. Убедитесь, что вы владелец файла (не редактор с доступом)
2. Проверьте, что Apps Script привязан к таблице (откройте через Расширения → Apps Script)
3. Попробуйте выйти и войти в Google аккаунт

---

## 📞 Проверка через Telegram API

Альтернативный способ проверить webhook:

1. Откройте браузер
2. Замените `YOUR_BOT_TOKEN` на ваш токен:
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getWebhookInfo
   ```
3. Проверьте ответ:
   - `"url"` должен содержать ваш webhook URL
   - `"pending_update_count"` должен быть `0`
   - `"last_error_message"` должно отсутствовать

---

## ✅ Успешный результат

После выполнения всех шагов:

1. ✅ Бот отвечает на `/start`
2. ✅ Бот отвечает на `/link КОД`
3. ✅ Диагностика не показывает ошибок
4. ✅ Ожидающих обновлений: 0

**Telegram Bot готов к работе!** 🎉

---

## 🆘 Если ничего не помогло

Напишите разработчику со скриншотами:

1. Скриншот развёртывания из Apps Script (Управление развёртываниями)
2. Скриншот диагностики из Law Table
3. Ответ от `getWebhookInfo` (см. выше)
