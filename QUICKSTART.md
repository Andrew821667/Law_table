# ⚡ Quick Start Guide

Быстрая настройка за 5 минут! 🚀

## 📝 Что у вас уже есть:

- ✅ Node.js и npm установлены
- ✅ clasp установлен (`sudo npm install -g @google/clasp`)
- ✅ clasp login выполнен (credentials в `~/.clasprc.json`)
- ✅ Script ID: `12yQOLe96OMg0wX_YfCwiqvvgXbba42-HYHouQFRqHl8O4mP2N_fq4-ET`

---

## 🚀 Шаг 1: Создайте репозиторий на GitHub

```bash
# Вариант A: Через веб-интерфейс
# Перейдите на https://github.com/new
# Создайте репозиторий с именем: legal-ai-sheets-scripts

# Вариант B: Через GitHub CLI (если установлен)
gh repo create legal-ai-sheets-scripts --public
```

---

## 📦 Шаг 2: На вашей локальной машине (macOS)

### 2.1. Скопируйте проект на локальную машину

Вы можете:

**Опция A:** Скачать проект как ZIP и распаковать

**Опция B:** Использовать готовую структуру (если проект уже есть на вашем Mac)

### 2.2. Инициализируйте Git

```bash
# Перейдите в директорию проекта
cd ~/legal-ai-sheets-scripts

# Инициализируйте Git
git init

# Добавьте remote (замените на свой репозиторий!)
git remote add origin https://github.com/Andrew821667/legal-ai-sheets-scripts.git

# Добавьте все файлы
git add .

# Первый коммит
git commit -m "feat: initial setup with Google Apps Script integration"

# Отправьте на GitHub
git branch -M main
git push -u origin main
```

---

## 🔐 Шаг 3: Настройте GitHub Secrets

### 3.1. Получите credentials

```bash
cat ~/.clasprc.json
```

**Скопируйте весь JSON!**

### 3.2. Добавьте в GitHub

1. Откройте https://github.com/Andrew821667/legal-ai-sheets-scripts
2. **Settings** → **Secrets and variables** → **Actions**
3. **New repository secret**
4. Создайте секрет:
   - Name: `CLASPRC_JSON`
   - Value: *вставьте JSON из предыдущего шага*
5. **Add secret**

---

## ✅ Шаг 4: Проверьте что всё работает

### 4.1. Локально (на вашей машине)

```bash
# Скачать текущий код из Google Sheets
npm run pull

# Проверить код
npm run lint

# Загрузить обратно
npm run push

# Открыть скрипт в браузере
npm run open
```

### 4.2. Проверьте CI/CD

```bash
# Сделайте любое изменение
echo "# Test" >> test.txt
git add test.txt
git commit -m "test: CI/CD check"
git push origin main
```

Перейдите на GitHub → Actions и посмотрите, как работает автодеплой! 🎉

---

## 🎯 Готово!

Теперь при каждом push в `main` код автоматически загрузится в Google Apps Script!

### Следующие шаги:

1. **Скачайте текущий код из Google Sheets:**
   ```bash
   npm run pull
   ```

2. **Изучите структуру:**
   - `src/Code.gs` - главные функции
   - `src/Utils.gs` - вспомогательные функции

3. **Внесите изменения и проверьте:**
   ```bash
   npm run lint
   npm run push
   ```

4. **Закоммитьте и запустите автодеплой:**
   ```bash
   git add .
   git commit -m "feat: добавил новую функцию"
   git push origin main
   ```

---

## 📚 Полезные команды

| Команда | Описание |
|---------|----------|
| `npm run pull` | Скачать код из Google Sheets |
| `npm run push` | Загрузить код в Google Sheets |
| `npm run lint` | Проверить качество кода |
| `npm run open` | Открыть скрипт в браузере |
| `npm run logs` | Показать логи |

---

## 🆘 Нужна помощь?

- 📖 Полная документация: [README.md](README.md)
- 🔧 Настройка GitHub: [SETUP_GITHUB.md](SETUP_GITHUB.md)
- ⚙️ Подробная инструкция: [GOOGLE_APPS_SCRIPT_SETUP.md](../legal-ai-website/GOOGLE_APPS_SCRIPT_SETUP.md)

---

**Happy coding!** 🎉
