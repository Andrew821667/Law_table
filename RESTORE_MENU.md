# 🔧 ВОССТАНОВЛЕНИЕ МЕНЮ В GOOGLE ТАБЛИЦЕ

## Что произошло?

Я ошибочно удалил папку `src/` с файлами Google Apps Script при миграции на Vercel.
**Теперь все восстановлено!** ✅

---

## ✅ Что восстановлено:

- **src/Main.gs** - главный файл с меню "⚖️ Судебные дела"
- **src/TelegramBot.gs** - интерактивный Telegram бот
- **src/CalendarManager.gs** - управление календарем
- **src/CaseManager.gs** - управление делами
- **src/FinancialManager.gs** - финансовый учет
- **src/LegalWorkflowManager.gs** - юридические функции
- **src/UserManager.gs** - управление пользователями
- И еще **19 модулей** системы!

---

## 📋 Деплой на Google Apps Script (5 минут)

### 1. Установите clasp (если еще нет)

```bash
npm install -g @google/clasp
```

### 2. Логин в Google Apps Script

```bash
clasp login
```

Откроется браузер → разрешите доступ к Google Apps Script.

### 3. Обновите репозиторий

```bash
cd /path/to/Law_table
git pull origin claude/investigate-deployment-issues-01CWX9N6HfvMAW3rjN6X24Cm
```

### 4. Деплой на Google Apps Script

```bash
clasp push
```

### 5. Откройте таблицу

Перезагрузите Google Таблицу → меню "⚖️ Судебные дела" появится!

---

## 🎯 Теперь у вас 2 способа работы:

### 1. **Google Sheets меню** (ПОЛНЫЙ функционал)
- Меню "⚖️ Судебные дела" в таблице
- Все функции: назначение дел, контроль сроков, финансы, отчеты
- Работает через Google Apps Script

### 2. **VPS бот** (упрощенный доступ)
- Telegram бот на вашем VPS
- Mini App + предстоящие заседания
- Работает через polling

**Оба способа работают с ОДНОЙ таблицей!**

---

## ❓ Если что-то не работает:

1. Убедитесь что `.clasp.json` содержит правильный `scriptId`:
```bash
cat .clasp.json
```

2. Если нужно создать новый проект Apps Script:
```bash
clasp create --type sheets --title "Law Table v2"
```

3. Привязать к существующему проекту:
```bash
clasp clone <YOUR_SCRIPT_ID>
clasp push
```

---

## 🎉 Готово!

После `clasp push` все меню в таблице появится автоматически!
