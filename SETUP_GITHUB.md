# Настройка GitHub репозитория и CI/CD

Пошаговая инструкция по настройке репозитория на GitHub и интеграции с Google Apps Script.

## 📋 Шаг 1: Создание репозитория на GitHub

### Вариант A: Через веб-интерфейс

1. Перейдите на https://github.com/new
2. Заполните форму:
   - **Repository name:** `legal-ai-sheets-scripts`
   - **Description:** "Google Apps Script для автоматизации работы с таблицами Legal AI"
   - **Visibility:** Public или Private (на ваш выбор)
   - ✅ **Add a README file** (можете снять галочку, т.к. README уже есть)
   - **Add .gitignore:** None (уже есть в проекте)
   - **Choose a license:** MIT (или выберите другую)
3. Нажмите **Create repository**

### Вариант B: Через GitHub CLI

Если у вас установлен `gh`:

```bash
# Создать публичный репозиторий
gh repo create legal-ai-sheets-scripts --public --description "Google Apps Script для автоматизации Legal AI таблиц"

# Или приватный
gh repo create legal-ai-sheets-scripts --private --description "Google Apps Script для автоматизации Legal AI таблиц"
```

---

## 📦 Шаг 2: Инициализация Git и первый push

### На вашей локальной машине (macOS):

```bash
# Перейдите в директорию проекта
cd /path/to/legal-ai-sheets-scripts

# Инициализируйте Git
git init

# Добавьте remote
git remote add origin https://github.com/Andrew821667/legal-ai-sheets-scripts.git

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "feat: initial project setup with clasp integration and CI/CD"

# Отправьте на GitHub
git branch -M main
git push -u origin main
```

---

## 🔐 Шаг 3: Настройка GitHub Secrets

GitHub Secrets нужны для автоматического деплоя через CI/CD.

### 3.1. Получение clasp credentials

На вашей локальной машине:

```bash
# Credentials находятся в файле
cat ~/.clasprc.json
```

**Скопируйте ВЕСЬ JSON** из этого файла. Он должен выглядеть примерно так:

```json
{
  "tokens": {
    "default": {
      "client_id": "...",
      "client_secret": "...",
      "type": "authorized_user",
      "refresh_token": "...",
      "access_token": "..."
    }
  }
}
```

### 3.2. Добавление секретов в GitHub

1. Перейдите в ваш репозиторий на GitHub
2. **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**

#### Секрет 1: CLASPRC_JSON

- **Name:** `CLASPRC_JSON`
- **Value:** Вставьте **весь JSON** из `~/.clasprc.json`
- Нажмите **Add secret**

#### Секрет 2: DEPLOYMENT_ID (опционально)

Если хотите обновлять существующий деплоймент (а не создавать новый каждый раз):

```bash
# На локальной машине
clasp deployments
```

Вывод будет примерно таким:
```
2 Deployments.
- AKfycbyXXXXXXXXXXXXXXXXXXX @1 - My Deployment
- AKfycbyYYYYYYYYYYYYYYYYYYY @HEAD - Latest version
```

Скопируйте ID деплоймента (строка начинающаяся с `AKfycby...`) и добавьте как секрет:

- **Name:** `DEPLOYMENT_ID`
- **Value:** `AKfycbyXXXXXXXXXXXXXXXXXXX`

**Примечание:** Этот секрет опционален. Если его нет, при каждом деплое будет создаваться новая версия.

---

## ✅ Шаг 4: Проверка CI/CD

### 4.1. Проверка workflows

После push'а в main:

1. Перейдите в **Actions** на GitHub
2. Должны появиться два workflow:
   - ✅ **Test & Lint**
   - ✅ **Deploy to Production**

### 4.2. Проверка деплоя

Workflow **Deploy to Production** должен:
1. ✅ Установить зависимости
2. ✅ Настроить clasp credentials
3. ✅ Загрузить код в Google Apps Script
4. ✅ Создать новую версию
5. ✅ Задеплоить на production

Если всё прошло успешно, вы увидите зелёную галочку ✅

### 4.3. Проверка в Google Sheets

1. Откройте вашу таблицу: https://docs.google.com/spreadsheets/d/1z71C-B_f8REz45blQKISYmqmNcemdHLtICwbSMrcIo8
2. Обновите страницу
3. Должно появиться меню **Legal AI** в верхней панели
4. Проверьте функционал через меню

---

## 🔄 Шаг 5: Workflow разработки

### Локальная разработка

```bash
# 1. Скачать последний код из Google Sheets
npm run pull

# 2. Внести изменения в src/

# 3. Проверить код
npm run lint

# 4. Загрузить в Google Sheets для тестирования
npm run push

# 5. Проверить в таблице

# 6. Если всё ОК - коммит и push
git add .
git commit -m "feat: add new feature"
git push origin main

# 7. CI/CD автоматически задеплоит на production
```

### Автоматический деплой

При каждом push в `main`:
1. GitHub Actions запустит workflow
2. Код автоматически загрузится в Google Apps Script
3. Создастся новая версия
4. Выполнится деплой на production

---

## 🐛 Troubleshooting

### Ошибка: "Invalid credentials"

**Решение:**
1. Проверьте `CLASPRC_JSON` secret в GitHub
2. Убедитесь, что скопировали **весь JSON** из `~/.clasprc.json`
3. Попробуйте пересоздать секрет

### Ошибка: "Script not found"

**Решение:**
1. Проверьте `.clasp.json` - правильный ли Script ID?
2. Убедитесь, что у аккаунта есть доступ к скрипту

### Ошибка: "Permission denied"

**Решение:**
1. Откройте таблицу в браузере
2. Extensions → Apps Script
3. Убедитесь, что вы залогинены под тем же аккаунтом, который использовали для `clasp login`
4. Проверьте права доступа к таблице

### Workflow не запускается

**Решение:**
1. Убедитесь, что workflows файлы находятся в `.github/workflows/`
2. Проверьте, что в Settings → Actions разрешены workflows
3. Проверьте синтаксис YAML файлов

---

## 🎯 Следующие шаги

После успешной настройки:

1. ✅ **Настройте branch protection** для `main`:
   - Settings → Branches → Add rule
   - Требуйте прохождения CI перед merge

2. ✅ **Создайте ветку `develop`** для разработки:
   ```bash
   git checkout -b develop
   git push origin develop
   ```

3. ✅ **Настройте Pull Request template**:
   - Создайте `.github/pull_request_template.md`

4. ✅ **Добавьте коллег** в репозиторий:
   - Settings → Collaborators

5. ✅ **Настройте уведомления** о деплоях:
   - Можно добавить интеграцию с Slack/Telegram

---

## 📚 Полезные команды

```bash
# Проверить статус CI/CD
gh run list

# Посмотреть логи последнего workflow
gh run view

# Запустить деплой вручную
gh workflow run deploy.yml

# Посмотреть список деплойментов в Google Apps Script
clasp deployments

# Откатиться на предыдущую версию
clasp versions
clasp deploy --versionNumber <VERSION>
```

---

## ✨ Готово!

Теперь у вас полностью настроен:
- ✅ GitHub репозиторий с версионированием
- ✅ CI/CD для автоматического деплоя
- ✅ Code quality checks (ESLint + Prettier)
- ✅ Автоматическое создание версий
- ✅ Интеграция с Google Apps Script через clasp

**Happy coding!** 🎉

---

Если возникнут вопросы или проблемы - создавайте Issue в репозитории! 🙌
