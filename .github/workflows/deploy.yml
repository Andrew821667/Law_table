name: Deploy to Apps Script

on:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - '.clasp.json'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install clasp
        run: npm install -g @google/clasp

      - name: Create clasprc.json
        run: |
          echo '${{ secrets.CLASPRC_JSON }}' > ~/.clasprc.json

      - name: Push to Apps Script
        run: clasp push --force

      - name: Deploy
        run: |
          DEPLOYMENT_ID="${{ secrets.DEPLOYMENT_ID }}"
          if [ -n "$DEPLOYMENT_ID" ]; then
            clasp deploy -i "$DEPLOYMENT_ID" -d "Auto deploy from GitHub - $(date)"
          else
            echo "DEPLOYMENT_ID not set, skipping deployment"
          fi

          echo "✅ Deployment completed successfully"

      - name: Setup Telegram Webhook
        if: success()
        run: |
          echo "🔧 Настройка Telegram webhook автоматически..."
          sleep 5
          curl -s "https://script.google.com/macros/s/AKfycbyFfwijoiLoXWxswMXD3kJX4Xq2VFh4bBfk2T24w58vADbUbmnB7FBCZCzs_kDVrvHCvA/exec?action=resetBot" || echo "⚠️ Webhook будет настроен при первом обращении"
          echo "✅ Webhook настроен"

      - name: Get deployment info
        if: success()
        run: |
          echo "### 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Status**: ✅ Success" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Time**: $(date +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📝 **Latest Commit**: ${{ github.event.head_commit.message }}" >> $GITHUB_STEP_SUMMARY
          echo "🤖 **Telegram Bot**: Webhook автоматически настроен" >> $GITHUB_STEP_SUMMARY

      - name: Notify on failure
        if: failure()
        run: |
          echo "### ❌ Deployment Failed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Please check the logs above for details." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Common issues:" >> $GITHUB_STEP_SUMMARY
          echo "- Invalid CLASPRC_JSON secret" >> $GITHUB_STEP_SUMMARY
          echo "- Missing permissions on Script" >> $GITHUB_STEP_SUMMARY
          echo "- Syntax errors in code" >> $GITHUB_STEP_SUMMARY
